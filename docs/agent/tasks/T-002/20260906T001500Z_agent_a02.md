# T-002: Слой БД — Выполнено

**Владелец:** agent
**Статус:** DONE
**Прогресс:** 7/7

## Что сделано

1. `src/db.py` — init_db() с CREATE TABLE users
2. create_user() — INSERT OR REPLACE
3. get_user() — SELECT by telegram_id
4. update_user() — UPDATE с динамическими полями
5. get_all_users() — SELECT ALL
6. get_users_by_role() — SELECT WHERE role = ?
7. Тесты пройдены: create, get, update, get_by_role — OK

## Проверка

```bash
.venv/bin/python -c "
import asyncio
from src.db import init_db, create_user, get_user, update_user, get_users_by_role

async def test():
    await init_db()
    user = await create_user(123, 'Тест', '09.03.01', 'seeker', 'frontend')
    print(f'Create: {user}')
    user = await get_user(123)
    print(f'Get: {user}')
    user = await update_user(123, name='Тест2')
    print(f'Update: {user}')
    users = await get_users_by_role('seeker')
    print(f'By role: {len(users)} users')
    print('All tests passed!')

asyncio.run(test())
"
```
