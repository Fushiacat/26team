# T-002: Слой БД

**Владелец:** agent
**Статус:** pending
**Этап PLAN.md:** Этап 1
**Время:** ~45 мин
**Зависимость:** T-001

## Описание

Реализовать модуль `src/db.py` для работы с SQLite: инициализация базы, CRUD-операции для таблицы users.

## Технические детали

### 1. Инициализация БД

Файл: `src/db.py`

```python
import aiosqlite
import os
from datetime import datetime

DB_PATH = "data/teammatch.db"

async def init_db():
    """Создаёт таблицу users, если не существует."""
    os.makedirs("data", exist_ok=True)
    async with aiosqlite.connect(DB_PATH) as db:
        await db.execute("""
            CREATE TABLE IF NOT EXISTS users (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                telegram_id INTEGER UNIQUE NOT NULL,
                name TEXT NOT NULL,
                direction TEXT NOT NULL CHECK(direction IN ('09.03.01', '09.03.03', '09.03.04', '27.03.04')),
                role TEXT NOT NULL CHECK(role IN ('seeker', 'organizer')),
                description TEXT NOT NULL,
                created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
            )
        """)
        await db.commit()
```

### 2. CRUD-операции

```python
async def create_user(telegram_id: int, name: str, direction: str, role: str, description: str) -> dict:
    """Создаёт или обновляет пользователя (INSERT OR REPLACE)."""
    async with aiosqlite.connect(DB_PATH) as db:
        await db.execute("""
            INSERT OR REPLACE INTO users (telegram_id, name, direction, role, description)
            VALUES (?, ?, ?, ?, ?)
        """, (telegram_id, name, direction, role, description))
        await db.commit()
        return await get_user(telegram_id)

async def get_user(telegram_id: int) -> dict | None:
    """Получает пользователя по telegram_id."""
    async with aiosqlite.connect(DB_PATH) as db:
        db.row_factory = aiosqlite.Row
        async with db.execute("SELECT * FROM users WHERE telegram_id = ?", (telegram_id,)) as cursor:
            row = await cursor.fetchone()
            return dict(row) if row else None

async def update_user(telegram_id: int, **fields) -> dict | None:
    """Обновляет поля пользователя."""
    if not fields:
        return await get_user(telegram_id)
    set_clause = ", ".join(f"{k} = ?" for k in fields)
    values = list(fields.values()) + [telegram_id]
    async with aiosqlite.connect(DB_PATH) as db:
        await db.execute(f"UPDATE users SET {set_clause} WHERE telegram_id = ?", values)
        await db.commit()
        return await get_user(telegram_id)

async def get_all_users() -> list[dict]:
    """Получает всех пользователей."""
    async with aiosqlite.connect(DB_PATH) as db:
        db.row_factory = aiosqlite.Row
        async with db.execute("SELECT * FROM users") as cursor:
            rows = await cursor.fetchall()
            return [dict(row) for row in rows]

async def get_users_by_role(role: str) -> list[dict]:
    """Получает всех пользователей с указанной ролью."""
    async with aiosqlite.connect(DB_PATH) as db:
        db.row_factory = aiosqlite.Row
        async with db.execute("SELECT * FROM users WHERE role = ?", (role,)) as cursor:
            rows = await cursor.fetchall()
            return [dict(row) for row in rows]
```

### 3. Зависимость: aiosqlite

Добавить в `pyproject.toml`:
```toml
dependencies = [
    "aiogram>=3.12,<4",
    "python-dotenv>=1.0",
    "aiosqlite>=0.20.0",
]
```

### 4. Обновить `src/bot.py`

Добавить вызов `init_db()` при старте:
```python
from src.db import init_db

async def on_startup():
    await init_db()

dp.startup.register(on_startup)
```

## Разрешённые файлы

- `src/db.py` (создание/редактирование)
- `src/bot.py` (редактирование — добавить on_startup)
- `pyproject.toml` (редактирование — добавить aiosqlite)

## Критерии проверки

- [ ] `python -c "import asyncio; from src.db import init_db; asyncio.run(init_db()); print('OK')"` — создаёт `data/teammatch.db`
- [ ] Таблица `users` существует в базе
- [ ] `create_user()` корректно сохраняет данные
- [ ] `get_user()` возвращает сохранённого пользователя
- [ ] `update_user()` обновляет указанные поля
- [ ] `get_users_by_role()` фильтрует по роли
- [ ] Повторный `create_user()` с тем же `telegram_id` обновляет запись (INSERT OR REPLACE)

## Блокер

T-001 (зависимости должны быть установлены)

## Последний файл выполнения

`src/db.py`

## Следующий шаг

T-003: Реализовать регистрацию /start с FSM
