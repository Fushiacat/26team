# T-003: Регистрация /start — Выполнено

**Владелец:** agent
**Статус:** DONE
**Прогресс:** 7/7

## Что сделано

1. `src/handlers/start.py` — RegistrationForm с 4 состояниями
2. /start — проверка существующей анкеты или запуск регистрации
3. process_name — валидация 2-50 символов
4. process_direction — inline-клавиатура направлений
5. process_role — inline-клавиатура ролей
6. process_description — валидация до 500 символов, сохранение в БД
7. `src/bot.py` — подключён start_router

## Проверка

```bash
.venv/bin/python -c "from src.bot import create_bot, dp; print('bot OK')"
```
