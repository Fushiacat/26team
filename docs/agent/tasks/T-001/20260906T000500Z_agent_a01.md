# T-001: Настройка проекта — Выполнено

**Владелец:** agent
**Статус:** DONE
**Прогресс:** 4/4

## Что сделано

1. `pyproject.toml` — добавлены зависимости: aiogram, python-dotenv, aiosqlite, pytest, pytest-asyncio
2. `.env.example` — инструкция по созданию токена через @BotFather
3. `.gitignore` — добавлены `.env` и `data/`
4. Структура директорий создана: `src/`, `src/handlers/`, `tests/`
5. `src/config.py` — загрузка BOT_TOKEN из .env
6. `src/bot.py` — create_bot() + dp (ленивая инициализация)
7. `src/keyboards.py` — direction_keyboard(), role_keyboard(), edit_keyboard()
8. `src/db.py` — init_db() с CREATE TABLE
9. `main.py` — точка входа с asyncio.run()
10. Зависимости установлены: `pip install -e ".[dev]"` — OK
11. Импорты проверены: config OK, db OK, bot OK

## Проверка

```bash
.venv/bin/python -c "from src.config import BOT_TOKEN; print('config OK')"
.venv/bin/python -c "from src.db import init_db; print('db OK')"
.venv/bin/python -c "from src.bot import create_bot, dp; print('bot OK')"
```
